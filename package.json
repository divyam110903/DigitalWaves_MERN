{
  "name": "pep-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run dev --prefix Backend\" \"npm start --prefix digitalwaves\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}